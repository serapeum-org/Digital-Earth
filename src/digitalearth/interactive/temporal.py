"""TemporalMixin — time-slider datacubes for :class:`~digitalearth.interactive.map.InteractiveMap`.

Will own: timecube over a DatasetCollection via hv.DynamicMap (DI.3).

Currently an **empty stub** (DI.0): methods land task by task; each builds a HoloViews/GeoViews element from
pyramids-sourced data and registers it via ``self.add_element(...)`` on the composed ``InteractiveMap``.
"""


class TemporalMixin:
    """Time-slider datacubes mixin (stub — methods land in later DI tasks)."""
